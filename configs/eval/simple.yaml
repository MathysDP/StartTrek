env:
  name: "LunarLander-v3"
  continuous: false
  gravity: -10.0
  enable_wind: false
  wind_power: 15.0
  turbulence_power: 1.5
  seed: 42

eval:
  episodes: 100

policy:
  type: "DQN"
  save_path: "train1.pth"
  hidden_sizes: [64, 128, 64]
  video_folder: "lunarlander-agent"
  video_prefix: "dqn"

log_path: "eval_log1.csv"